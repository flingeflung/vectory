{{--
    Ralf, 2026-09-11: Platzhalter nach Vietto-Vorbild (dort "Erstellungsstatus",
    Neuerstellung/Änderung) - in Vectory noch ohne eigene Datenquelle/Eingabe,
    zeigt bewusst nur einen Hinweis statt eines echten Werts.
--}}
<div>
    <label class="block text-xs text-gray-500">{{ __('Erstellungsstatus') }}</label>
    <div class="mt-0.5 text-gray-400">{{ __('– noch nicht verfügbar –') }}</div>
</div>
